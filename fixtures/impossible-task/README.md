# Impossible task fixture

A fixture whose test suite demands a general halting decision procedure. No candidate can satisfy it, so the run must end as exhausted with a recorded exclusion reason for every candidate rather than fabricating a pass.
